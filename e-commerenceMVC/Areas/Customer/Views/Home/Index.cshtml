@using Ecommerence.Models
@model IEnumerable<Product>
<div class="row pb-3">
	@foreach(var product in Model)
	{
        
        <div class="col-lg-3 col-sm-6 h-100">
            <div class="row p-2 h-100">
                <div class="col-12 p-1 h-100">
                    <div class="card h-100 border-0 p-3 shadow border-top border-5 rounded">
                        <img src="@product.ImageUrl" class="card-img-top rounded" style="height: 250px; object-fit: cover;" />
                        <div class="card-body pb-0 d-flex flex-column justify-content-between">
                            <div class="pl-1">
                                <p class="card-title h5 text-dark opacity-75 text-uppercase text-center">@product.Title</p>
                                <p class="card-title text-warning text-center"><b>@product.Author</b></p>
                            </div>
                            <div class="pl-1">
                                <p class="text-dark text-opacity-75 text-center"><span>@product.Price.ToString("c")</span></p>
                            </div>
                        </div>
                        <input type="hidden" asp-for="@product.ProductId" />
                        <a asp-action="Details" asp-route-productId="@product.ProductId" class="btn btn-primary bg-gradient border-0 form-control">
                            Detay
                        </a>
                    </div>
                    
                </div>
            </div>
        </div>

	}
</div>